<<<<<<< HEAD
version: "3.8"

services:
  # 1. API Backend (Node.js)
  api:
    build:
      context: server
      dockerfile: ../server.Dockerfile
    environment:
      - PORT=3001
      - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - VITE_GEMINI_API_KEY=${VITE_GEMINI_API_KEY}
    networks:
      - prospector-net
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    volumes:
      - ./server/logs:/app/logs

  # 2. Worker (Fila de Jobs)
  worker:
    build:
      context: server
      dockerfile: ../server.Dockerfile
    command: ["node", "worker.js"]
    environment:
      - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - VITE_GEMINI_API_KEY=${VITE_GEMINI_API_KEY}
    depends_on:
      api:
        condition: service_healthy
    networks:
      - prospector-net
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    volumes:
      - ./server/logs:/app/logs

  # 3. Frontend (Nginx Server)
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - prospector-net
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M

networks:
  prospector-net:
    driver: bridge
=======
version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - backend
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  backend:
    build: ./server
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      # Assuming standard Postgres for demo, replace with Supabase credentials if needed
      - DATABASE_URL=postgres://postgres:postgres@db:5432/postgres
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./server/logs:/app/logs
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  db_data:
>>>>>>> main
